{
  "name": "portfolio-cv",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "sass src/scss/main.scss assets/css/styles.css --watch",
    "build": "sass src/scss/main.scss assets/css/styles.css --style=compressed --no-source-map",
    "start": "serve . -l $PORT",
    "preview": "serve . -p 3001"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "sass": "^1.80.0"
  }
}
